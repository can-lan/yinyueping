<!--歌单组件-->
  <view class="sheets">
    <!--歌手-->
    <view class='songSheets' wx:for='{{singer}}' wx:key='{{id}}' data-id='{{id}}'>
      <image src="{{item.img}}" mode="widthFix"></image>
      <text>{{item.singer}}</text>
    </view>
  </view>
  