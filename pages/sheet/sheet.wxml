  <view class="sheets">
    <!--歌单组件-->
    <view class='songSheets' wx:for='{{list}}' wx:key='{{id}}' data-id='{{id}}'>
      <image src="{{item.img}}" mode="aspectFit"></image>
      <text>{{item.title}}</text>
    </view>
  </view>
  